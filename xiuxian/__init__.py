"""文字修仙 —— 可扩展的文字修仙游戏框架。

分层约定：
    config/   纯数据（境界、物品），改数值不用动逻辑
    core/     引擎内核（属性、角色、修炼、事件、背包、存档、主循环）
    systems/  可插拔玩法模块（战斗、渡劫、门派……），通过 GameSystem 挂载
    ui/       表现层（终端 CLI），可替换成 Web / GUI 而不动内核

依赖方向：ui -> systems -> core -> config，不允许反向依赖。
"""

__version__ = "0.1.0"
