# 文字修仙 —— 交付说明

## 第一轮：基础框架

在空目录 `~/WorkBuddy/文字修仙` 下从零搭建了一套可运行的文字修仙游戏框架，纯 Python 标准库，无第三方依赖。

**已实现（7 个基础模块）**

- 境界体系：九个大境界（炼气 / 筑基 / 金丹 / 元婴 / 化神 / 炼虚 / 合体 / 大乘 / 渡劫），炼气九层、其余分初期~圆满，修为需求按指数曲线生成
- 属性面板：气血、灵力、攻击、防御、身法、神识、悟性、根骨、气运，采用 `base + Modifier(add/mul)` 双层修正
- 修炼与突破：吐纳产修为、精力约束、顿悟概率；分小境界突破与大境界突破两套判定，失败有反噬
- 剧情事件：数据驱动，9 个事件写在 `data/events.json`，支持境界/地点/状态/flag 条件与选项前置
- 物品与丹药：8 类物品，丹毒机制抑制无脑嗑药，突破丹提供定向成功率加成
- 存档读档：JSON + 版本号 + 迁移钩子 + 原子写入，RNG 状态一并持久化
- 主循环与 CLI：时间推进、跨日结算、寿元判定、命令解析

**已实现（3 个示范扩展系统）**

- 战斗：回合制斗法，敌人按玩家属性等比缩放，濒死护持不死；注册 `battle` 效果供事件调用
- 渡劫：金丹以上突破前独立判定天劫，未渡则中断突破（订阅 `before_breakthrough` 实现）
- 门派：入门、日俸、贡献、职位晋升（演示完整接入五要素）

## 关键设计决策

1. **突破判定通过事件总线开放**：掷骰前广播 `before_breakthrough`，外部系统可叠加成功率或直接拦截。丹药、渡劫、门派互不感知，各自只改 `payload`。
2. **效果 DSL 统一结算**：事件、丹药、战斗掉落、任务奖励共用一套 `{"type": ...}` 语法，新增玩法无需新增结算代码；不够用时可 `register_effect` 注册自定义类型。
3. **依赖单向**：`ui → systems → core → config`，内核不知道玩法系统的存在，装卸载玩法只改 `factory.default_systems()` 一行。
4. **RNG 状态可存档**：读档后随机序列无缝延续，固定 seed 可完整复现。

## 验证结果

- `demo.py`（seed=42）完整跑通：修炼 → 突破 → 机缘事件 → 斗法 → 嗑药 → 存档 → 读档一致性校验通过
- `python -m unittest discover -s tests`：28 项内核测试全部通过
- 长线自动推演（seed=2026）：第 115 日筑基圆满、第 162 日金丹中期，天劫链路按预期触发并给出成败反馈
- 交互模式全命令冒烟通过：travel / explore / choose / hunt / sect join / save / load / slots

## 过程中修正的问题

- 丹药突破加成原按「背包全部累加」计算且从不消耗，改为按目标境界匹配、同种最多叠 2 颗、成功才扣除
- 跨境界突破原用旧境界属性成长再乘系数，改为直接取新境界参数（数值天然形成量级跃迁）
- 渡劫裸装成功率仅 33% 且失败纯惩罚，调整为约 45% 并加入「劫后余生，神识 +1」补偿
- 突破被拦截时不消耗时间和精力，可无限重试刷判定；已补上 1 小时时间推进
- 修为圆满后继续修炼会产生「修为 +0」噪音日志，已过滤零值变化

---

## 第二轮：装备 + 功法（第一批扩展）

按 `docs/roadmap.md` 的第一批实施，用户选定：范围「装备 + 功法」、装备做「简版纯属性」、秘境（第三批）定「线性层数推进」。

### 新增模块

**装备系统** `systems/equipment.py` — 六个部位（兵器/护甲/靴履/佩饰/腰带/法宝），穿戴即注入属性，卸下即失效。越阶装备（玄铁重剑需筑基）穿不上；同部位换装自动卸下旧的回背包；部位名支持中文（「护甲」）与键名（`armor`）。

**功法系统** `systems/arts.py` — 6 门功法分黄/玄/地/天四阶。核心是**熟练度缩放**：到手只有六成效力，练满才十成，因此「攒钱跳最高阶」不等于立刻变强。高阶功法 `practice_gain` 更低（天阶 ×0.5、黄阶 ×1.4），练成耗时差三倍多，形成「早换早受益 vs 高阶后劲足」的取舍。

两者共用同一套机制：**挂载 → 注入 Modifier → 按 source 前缀幂等重建**。

```
最终属性 = (base + Σadd) × Πmul

穿青锋剑   -> add_modifier(Modifier(source="equip:weapon", add={"atk": 6}))
主修清心诀 -> add_modifier(Modifier(source="art:main", add={"max_mp": 12},
                                    mul={"comprehension": 1.09}))
```

战斗、修炼、渡劫读的都是 `p.atk` / `p.max_hp` 这类派生属性，因此装备与功法**零适配代码**即自动生效。前缀的意义在于读档后可整批清掉旧修正器再重建，避免残留不一致。

### 内核为此做的三处改动

1. `attributes.py` 加 `remove_modifiers_with_prefix` — 按前缀批量移除，供幂等重建
2. `base_system.py` 加 `TOPIC_PRACTICE` — 原本计划功法熟练度挂在 `hour_passed` 上，但 `hour_passed` 在赶路、休息、战斗后都触发，只有新主题才精确对应「玩家主动吐纳」
3. `cultivator.py` 加 `equipment` 字段，初始灵石 100 → 200

### 实施中发现并修掉的五个问题

| 问题 | 根因 | 修法 |
|---|---|---|
| 熟练度涨了但玩家看不到 | `bus.emit()` 不返回 handler 返回值 | 订阅者把文本塞进 `payload["logs"]`，广播方统一收回 |
| 一天内可反复冲天劫 | 渡劫失败走 `blocked` 拦截分支，不经 `_fail_penalty`，不设冷却 | 拦截分支补冷却 |
| 越冲关越难，最终卡死 | 无冷却时失败 +3 丹毒、每日仅消退 -1，负反馈 | 加冲关冷却（大境界 3 日 / 小境界 1 日），而非调数值 |
| 攒不出钱买功法 | 战斗灵石 `exp_reward/20`，早期一场 1 灵石，功法要 200 | 改为 `exp_reward/10 + 8×难度系数` |
| 斗法日志「第击要害」、敌人显示残血 0 却仍在出手 | 错字 + `f"{0.3:.0f}"` 显示成 0 | 修正错字；残血向上取整 |

另补：残血（<40%）冲关只预警不拦截——残血冲关是玩家自己的选择，但得让他知道代价。

### 验证

- **44 项单元测试通过**（原 28 + 装备 6 + 功法 7 + 冲关冷却 3）
- `demo.py`（seed=42）跑通含功法装备的完整链路，存档读档一致性校验通过
- 长线推演（seed=2026，含打猎攒钱与换装）：第 39 日筑基圆满、第 45 日金丹初期、第 88 日元婴初期；六门功法全练满；冲关失败间隔严格 3 日
- 交互模式全命令冒烟通过：art / art list / art learn / art practice / equip / unequip

### 后续

第二批炼丹、第三批秘境（已定线性层数）、第四批任务与坊市。坊市落地前，后期灵石会溢出（元婴期已积累 5 万+）。

---

## 第三轮：炼丹（第二批扩展）

用户选定：**带失败率、不炸炉**；丹药**分下品 / 中品 / 上品**（药效 70% / 100% / 140%）。

### 闭环

```
dan gather 采药（灵草，凶险处另有妖丹 / 灵泉水，并可能遇险掉血）
   ↓
dan learn  换购丹方（开局只会聚气、疗伤两张，其余用灵石买）
   ↓
dan refine 开炉：成功率掷点 → 成丹 → 品质值定下/中/上品
   ↓
嗑药冲关（上品药力猛，丹毒也猛；丹毒积压了就炼清毒丹）
```

### 三处可解释的公式（每次开炉都会把算式打在日志里）

```
成功率 = 夹[5%,95%](丹方基础 + 造诣×2% + (悟性−10)×1.5% + 气运×0.4% + 丹炉加成)

品质值 = (成功率−掷点)/成功率 + 火候修正(造诣×2% + 丹炉，上限 +20%)
         ≥66% 上品　≥33% 中品　否则下品

失手   = 只损材料（每种退回一半，向下取整）＋ 三成修为 ＋ 0.4 点造诣
```

**成败看悟性气运，火候看手艺与家伙** —— 这个分工是刻意的。若品阶只由归一余量决定，它必然均匀分布，造诣（上限 +24%）和丹炉（+6% / +12%）对品阶就毫无影响，只剩「少废几炉」的意义。实测：零造诣无丹炉时三品各 1/3；满造诣配朱雀丹炉后上品过半、下品近乎绝迹。

### 品阶不手抄三份表

`config/items.py` 只写中品基表，下品/上品用 `dataclasses.replace` 程序化派生（id 带 `#low` / `#high`），药效、丹毒、价格、突破加成按倍率一起缩放。**中品即基表、id 不变**，老存档、掉落表、事件 JSON 里的物品 id 一个都不用改 —— 品阶对既有内容零侵入。

### 新增内容

- 7 张丹方：聚气 / 疗伤 / 清毒 / 锻骨 / 悟道 / 筑基 / 结金（后五张需灵石换购，合计约 9400 灵石，是后期灵石的去处之一）
- 1 味新丹：清毒丹（不进药力，专化丹毒，是丹毒负反馈的泄压阀）
- 2 件丹炉：紫砂药鼎（+6%）/ 朱雀丹炉（+12%，需筑基），占法宝位，与青囊葫芦二选一
- 1 个事件：「丹房遗册」——丹方 / 药鼎 / 灵草三选一
- 效果 DSL 新增 `recipe` 类型（事件可直接传丹方）与负值 `poison`（化毒）

### 修掉的三个真 bug

| 问题 | 根因 | 修法 |
|---|---|---|
| 推演到渡劫后期必崩 | `spawn()` 用 `exp_required()` 作战利基数，满级时是 `inf`，`int(inf)` 抛 `OverflowError` | 退回上一层需求 |
| 后期灵石一场掉上亿（推演结余 288 亿） | 第一批把灵石挂 `exp_reward/10`，而修为需求随境界指数膨胀 | 与修为脱钩：`(8 + 6×难度) × 1.6^境界序号`，炼气约 14、渡劫约 600，结余降到 13 万 |
| 清毒丹日志显示「丹毒 +-21」 | `poison` 效果按正值格式化，传负值就叠出 `+-` | 分正负两支 |

另修：背包品阶标签重复显示（名称与描述里已有，面板不再加）、批量炼制「停火」时收尾文案看不出实际开了几炉。

### 验证

- **64 项单元测试通过**（原 44 + 品阶 7 + 丹道 13）
- `demo.py`（seed=42）跑通「采药 → 换购丹方 → 开炉 → 服上品丹」链路，存档读档一致性校验通过
- 交互模式全命令冒烟通过：`dan` / `dan list` / `dan gather` / `dan learn` / `dan refine` ×2
- 长线推演（seed=2026 / 7，400 日，含猎妖与炼丹）：第 277 / 259 日渡劫后期走完全程；各开炉 680 / 713 次，成丹率 92% / 93%；七张丹方买齐

### 后续

第三批秘境（已定线性层数推进）、第四批任务与坊市。坊市落地前后期灵石仍会溢出（400 日推演结余 13 万）。

---

## 第四轮：秘境（第三批扩展）

形态按既定决策：**线性层数推进**（非网格地图）。三座秘境按境界门槛解锁：落云（炼气，3 层）、万兽（筑基，5 层）、太虚（金丹，7 层），全部写在 `config/dungeons.py` 纯数据层，加新秘境不改代码。

### 四条规则

1. **层内抽签** —— 每层按权重抽「妖兽 / 机缘 / 遗宝 / 静室」，最后一层固定守关之敌。机缘层走 `EventSystem` 且限定 `scene: dungeon`，与户外事件池隔离（洞天里不会撞见「宗门招徒」）
2. **进度可续** —— `dungeon flee` 随时退出，回来 `dungeon enter` 从原层继续；通关进入冷却（3 / 5 / 7 日）；`dungeon abandon` 可放弃进度换取提前重入
3. **战败退一层** —— 濒死护持后被打退一层，已得之物不吐出来、不掉装备境界；退层即整备（回 25% 气血）防残血连败死亡螺旋；连败三次明说「此路不通」
4. **奖励走效果 DSL** —— 宝箱与通关大奖复用 `core/effects.py`；修为一律用 `exp_ratio`（按当前境界需求比例），不用绝对值——固定值在炼气期能直接飞升、到渡劫期等于零

### 关键设计：难度锚点是 tier 不是层数

敌人属性按玩家境界等比缩放（`combat.spawn`），所以「第 7 层」和「第 1 层」若同 tier，打起来一样难，层数就成了摆设。秘境难度只由层内 `tier`（weak → normal → elite → boss）拉开，层数只是 tier 的载体。炼气秘境的守关只用 elite 不用 boss——boss 档（2.6 倍）在低境界几乎必败，与境界无关。

### 战场信息失真的两个坑（实现时踩的）

| 坑 | 解法 |
|---|---|
| `fight()` 只返回日志、不返回胜负 | 秘境订阅 `combat_victory` 总线标记胜负；战后气血低于 11.5% 判「败退」而非「平局」 |
| 深入前精力不足，`fight()` 直接 return 且敌人满血存活 | 会被误判成「战败退一层」——深入前自己先查精力门槛 |

### 新增内容

- `config/dungeons.py`：三座秘境 + 三档宝箱池 + 通关大奖（都含 `exp_ratio` 修为与灵石/材料/丹药）
- `systems/dungeon.py`：进入 / 深入 / 退层 / 退出 / 放弃 + `dungeon` 效果与命令族
- `core/event_system.py`：`SCENE_DUNGEON` 场景标签
- `data/events.json`：3 个秘境事件（无字石碑 / 前人遗骸 / 残阵），`conditions: {"where": "dungeon"}`
- `demo.py`：新增「4.8 秘境探索」环节

### 验证

- **83 项单元测试通过**（原 64 + 秘境 19 项）
- `demo.py`（seed=42）跑通含秘境链路，存档读档一致性校验通过
- 交互模式冒烟通过：`dungeon list` / `enter` / `next`（战斗层推进）/ `flee`（进度保留）/ `info`
- 修一处测试断言：`run` 结构加了 `streak`（连败计数）字段，手写 run 的全等断言补上 `"streak": 0`

### 后续

第四批任务与坊市（见第五轮）。秘境宝箱给后期灵石多了一个去处。

---

## 第五轮：任务 / 坊市（第四批扩展）

按 `docs/roadmap.md` 的第四批实施，用户未单独拍板细节，按既定方案落地：**任务给修炼之外的目标感、坊市给灵石消费出口**。

### 任务系统 `systems/quest.py` + `data/quests.json`

5 个任务纯数据写在 `data/quests.json`，加载即生效、加任务不碰代码：

| 任务 | 门派 | 目标 | 奖励 | 声望 | 门槛 |
|---|---|---|---|---|---|
| 斩妖卫道 | 青云宗 | 战斗 ×3（min_tier=normal） | 灵石 150 + exp_ratio 3% | 8 | 可重复 |
| 寻仙访道 | 青云宗 | 事件 ×2 | 灵石 100 + 聚气丹 ×2 | 6 | 可重复 |
| 猎杀凶兽 | 青云宗 | 战斗 ×1（min_tier=elite） | 灵石 400 + 妖丹 ×1 | 12 | 可重复 |
| 破境之资 | 散修 | 突破 ×1 | 灵石 300 + 神识 +1 | 5 | 一次性 |
| 宗门荣耀 | 青云宗 | 战斗 ×10（min_tier=normal） | 灵石 1000 + exp_ratio 15% + 根骨 +2 | 20 | 一次性；需青云宗声望 24 |

**设计取舍**：开局自动接取所有可用任务（无手动 accept），玩家的目标感来自「手头永远有活干」；目标达成即发奖励并按门派累加声望，可重复任务立即续接、一次性任务永久封档。声望跨门槛原本只在新档/读档时接取——已在 `_complete()` 末尾补 `_auto_accept()`，使「宗门荣耀」这类高阶任务在声望攒够时自动接进，形成「做任务涨声望 → 解锁更肥任务」的正循环。

### 坊市系统 `systems/market.py`

让灵石有稳定出口：`spirit_stones` 与 `ItemDef.price` 已存在，坊市只做价格浮动与买卖。

- **每日浮动价**：订阅 `day_end`，价格按 `rng.between(-0.15, 0.15)` 漂移并夹在基准价 `[0.5, 1.8]` 倍，避免刷成 0 或天价
- **卖出价 = 买入价 × 0.6**（四舍五入），定位是「回收而非套利」
- **买卖不耗时间**：与修炼/秘境脱钩，坊市只是资源转换站，不抢修炼节奏
- 命令：`market list` / `market buy <id> [数量]` / `market sell <id> [数量]`

### 内核为此做的改动

- `systems/combat.py`：`Enemy` 加 `tier` 字段；`_victory` 在 `combat_victory` 广播 payload 加 `"tier": enemy.tier`，供任务按难度档过滤（如「猎杀凶兽」要 elite）
- `ui/panel.py`：状态面板加「任务 进行中数　声望 各门派」行
- `factory.py`：`default_systems()` 加 `QuestSystem()` 与 `MarketSystem()`

### 修掉的三个问题

| 问题 | 根因 | 修法 |
|---|---|---|
| `_quest_line` NameError | 列表推导变量名写错 `q` | 改为 `[qid for qid, st in ...]` |
| 测试连打妖精力耗尽卡计数 | 战斗 `FIGHT_STAMINA=25`，连打 3~10 次后精力耗尽、`fight` 直接 return 不触发胜利 | fighting 循环内重置 `p.stamina = 100` |
| 测试「买失败 count==0」断言错位 | 开局自带 3 颗聚气丹，买失败时 count 是 3 不是 0 | 断言「等于买前 count」 |

### 验证

- **100 项单元测试通过**（原 83 + 任务 9 + 坊市 8）
- `demo.py`（seed=42）跑通「任务概览 → 猎妖交付斩妖卫道 → 坊市买丹/卖妖丹」链路，存档读档一致性校验通过
- 交互模式冒烟通过：`quest` / `market list` / `market buy` / `market sell`
- 长线推演（seed=2026 / 7，400 日，含任务 + 坊市）：两 seed 均第 ~244 日达渡劫后期、无崩溃；青云宗声望 1400+、散修 5、一次性完成 2、进行中 3、买入 40 次、卖出 85 次；灵石结余从纯秘境轮的约 1.2 万量级被进一步消纳，溢出明显收敛

### 后续

第五批候选：NPC 对话树、自由地图探索网格、装备强化 / 套装、功法主动技能。框架的扩展点（GameSystem + 事件总线 + 效果 DSL + Modifier）都已验证可低成本接入，新增玩法仍只需改 `factory.default_systems()` 一行。

---

## 第六轮：功法主动技能（第五批扩展）

用户选定：**功法主动技能**，施法模型定 **自动施法 + 策略预设**（不碰 `fight()` 的非交互契约）。把「灵力」这条线接活——此前功法只影响属性，`p.mp` 在战斗中完全闲置，尽管配置一直在堆 `max_mp`（清心诀 +20、太虚养神功 ×1.30、大衍周天诀 ×1.12）。

### 核心机制

**修习功法即得签名技能**，威力按这门功法的熟练度缩放（复用 `proficiency_scale`：刚学会六成、练满十成），与属性加成的取舍逻辑完全一致。

| 功法 | 签名技能 | 类型 | 消耗 | 冷却 | 满熟练威力 |
|---|---|---|---|---|---|
| 清心诀 | 清心咒 | 疗伤 | 灵力 18% | 3 回合 | 回复气血 22% |
| 庚金剑诀 | 庚金一击 | 攻伐 | 灵力 10% | 2 回合 | 190% 普攻 |
| 玄龟镇海功 | 玄龟护体 | 护体 | 灵力 12% | 2 回合 | 减伤 45% |
| 赤焰焚天诀 | 焚天斩 | 攻伐 | 灵力 16% | 4 回合 | 260% 普攻（自伤 6%） |
| 太虚养神功 | 太虚回灵 | 回灵 | 不耗灵力 | 5 回合 | 回复灵力 28% |
| 大衍周天诀 | 周天星斗 | 攻伐 | 灵力 20% | 3 回合 | 290% 普攻 |

**为什么不能回合手动选技能**：`fight(enemy) -> list[str]` 一次调用打完全场，调用方有四处——事件 `{"type":"battle"}`（自动开战无处输入）、`hunt`、`dungeon.next()`、测试与推演脚本。改成回合手动会让这四处全部失效。因此决策权交给玩家预设的策略，战斗仍是一次调用打完。

### 三档策略

| 策略 | 治疗线 | 护体线 | 灵力保留 | 推演表现（seed=2026，400 日） |
|---|---|---|---|---|
| 保守 | 气血 55% | 70% | 留 40% 不动 | 零战死，但 ~2% 战斗打满 40 回合未分胜负 |
| 均衡 | 35% | 45% | 留 20% | 比无技能快约 15%，无战死 |
| 激进 | 25% | 不放护体 | 倾泻 | 结束最快，seed=2026 出现过 1 次战死 |

### 三条反模式防线

1. **消耗与产出一律按比例，不写绝对值** —— 灵力上限跨五个数量级（开局 40 → 渡劫 1173 万），写 `mp_cost=20` 在炼气是 9% 蓝条、渡劫只剩 0.0002%，与「灵石挂指数结余 288 亿」「秘境修为写固定值」同一类坑。按比例后实测庚金一击在九个境界都恰好是蓝条 10.0%。
2. **回灵零耗** —— 它是「灵力见底」死局的出口，代价是让掉一回合，必须恰好在蓝空时能放出来。
3. **两处「不浪费回合」判定** —— 敌人残血（<20%）不放护体（减伤救不了人，多点输出才能结束战斗）；攻击技能全在冷却且药力会溢出时，满血不疗伤、满蓝不回灵，宁可普攻（否则日志出现「气血回复 1（60/60）」）。

### 内核改动

- `config/arts.py`：`ArtDef` 加 `skills: tuple[str, ...] = ()`（frozen dataclass 默认值，老功法与新档零侵入，不升 `SAVE_VERSION`）
- `config/skills.py`：新建技能表
- `systems/skill.py`：新建 `SkillSystem` —— 只管「决策 + 付费 + 计时」，返回计划给战斗系统结算，不持有 enemy
- `systems/combat.py`：`_player_strike` 先问技能系统、`_enemy_strike` 消费护体减伤、开战/每回合/战后三处节拍钩子 —— 全部 `game.systems.get("skill")` 可选调用，**取不到就完全按原样普攻**
- `ui/panel.py`：状态面板加「技能 名称列表（施法策略：X）」行
- `factory.py`：`default_systems()` 在 `ArtSystem()` 后插一行 `SkillSystem()`

### 修掉的五个问题

| 问题 | 根因 | 修法 |
|---|---|---|
| `fight()` 钩子缩进错误 | 插入钩子时缩进写成模块级 | 重写整块为 8/12 空格缩进，84 个连锁报错恢复 |
| 战后 skill 面板挂着「冷却中 剩 N」 | 冷却只在开战清空 | 加 `end_battle()` 战后清空（连 `_guard` 归零） |
| 满血放疗伤白让回合 | 攻击技能进冷却后无脑补辅助 | 加 `_worth()`：八成药力落不进去就普攻 |
| 保守档 ~3% 战斗打满 40 回合 | 残血还在放护体 | 敌人 <20% 血任何策略都不护体（seed=2026 超时 246→149，seed=7 18→9） |
| 推演脚本两个坑（非游戏 bug） | 学功法门槛 4 万灵石 + 精力不足 30 空转 | 按单价 2 倍门槛、精力 ≥30 才冲关 |

### 验证

- **118 项单元测试通过**（原 100 + 技能 18），含 `test_combat_unchanged_without_skill_system` 守着可选装配降级
- `demo.py`（seed=42）跑通含技能链路（查看 → 学庚金剑诀 → 战中出现「施展【庚金一击】」→ 切激进 → 非法策略回退），存档读档一致性校验通过
- 交互模式冒烟通过：`skill` / `skill strategy 保守` / 非法输入 / `hunt` 战后冷却清空
- 长线推演（seed=2026 / 7，含猎妖 + 六功法全修 + 三策略 + 无技能对照）：均推进到渡劫后期、无崩溃；每场施法 10~12 次；均衡/激进档战斗回合比无技能少约 15%，保守档零战死但更长——「更快 vs 更稳」取舍成立，技能对进度曲线无破坏性影响

### 后续

第六批候选：NPC 对话树、自由地图探索网格、装备强化 / 套装、战斗道具（战斗中使用丹药/符箓）。技能系统已为「每门功法多技能、玩家自选配招」留好扩展空间（`ArtDef.skills` 是元组，`choose_action` 只看 kind 与 power）。

---

## 第七轮：文字质感（第六批扩展）

用户明确「核心玩法是以文字为重点的」，以《想不想修真》为参照。经对齐，选定三样分批落地：**文字质感 → 图鉴 → 挂机**，本轮只做文字质感。核心原则一句话：**数值与判定是骨架，文案是血肉，两者严格分离——换文风只改数据层，不动一行逻辑。**

### 战斗日志叙事化

战斗不再是数值简报（「你击中 青纹狼，造成 12 点伤害」），而是每回合从词库抽动作前缀的叙事流：

```
寒光乍现，你击中 青纹狼，造成 13 点伤害，剩余 30
你施展【庚金一击】，一道庚金剑气凝于剑尖，锐啸破空，直贯 青纹狼 要害。青纹狼 受到 22 点伤害，剩余 0
青纹狼 发出一声悲鸣，缓缓倒地不起。
```

词库（`systems/combat.py` 顶部四个常量）可自行扩充，加条目即生效。

### 三处文案落点（全部在数据层）

| 落点 | 位置 | 说明 |
|---|---|---|
| 技能施放文案 `cast` | `config/skills.py` | 6 门签名技能各一条；attack 类支持 `{enemy}` 占位（`str.format`，非 f-string）；未配置自动退回通用句式 |
| 境界铭文 `REVELATIONS` | `config/realms.py` | 九个大境界各一句，跨大境界突破时插入（「金丹碎裂，一道元婴自识海踏出…」）；小境界不打扰 |
| 战斗词库 | `combat.py` 顶部常量 | 普攻/暴击/受击/胜利四个池子，可扩充 |

### 锚点词契约（测试守着）

文案随便改，但保留机器依赖：普攻行含「你击中」、技能行含「施展【」、受击行含「反扑/受到/剩余」、胜利行含「倒地不起」。词库与判定共用 `game.rng`，同 seed 依旧完全可复现；相对第五批的随机序列整体偏移（推演细节数字变了），但数值公式与判定规则零改动。

### 验证

- **126 项单元测试通过**（原 118 + TestProse 8 项）
- `demo.py`（seed=42）跑通，战斗日志呈叙事流，存档读档一致性「通过」
- 交互冒烟：hunt 叙事化、技能施放渲染 cast 文案
- 长线推演（seed=2026 / 7，三策略 + 无技能对照）：行为结构与第五批一致，无系统性偏差

### 后续

第七批候选：**图鉴收集系统**（传记/见闻录/神通，随事件、战斗、突破解锁条目，未解锁留悬念占位）→ 第八批**挂机与离线收益**。之后仍有 NPC 对话树、自由地图探索网格、装备强化 / 套装、战斗道具。技能系统已为「多技能配招」留好扩展空间。

## 第七轮：仙界九境 + 飞升 + 境界神通 + 突破率修正

产品定位转为**挂机养老**（取消寿元死亡判定），飞升不是终点——「法无止境，仙是修不完的」。用户经调研确认洪荒九阶仙界，本批落地。

### 机制
- **凡九 + 仙九 = 十八境**：渡劫圆满突破即飞升（专属横幅 + 铭文），进入人仙 → 地仙 → 天仙 → 玄仙 → 金仙 → 太乙 → 大罗 → 准圣 → 混元。仙界就是 realms.py 追加的九行数据，飞升零特殊分支
- **仙劫分层**：仙界跨境界渡劫沿用同一判定，劫名按目标境界数据驱动（仙劫 → 天衰之劫 → 道心劫 → 斩三尸 → 合道大劫）
- **境界神通**：金丹起每境一个数据驱动被动（POWERS 表），数值型走 Modifier(source="realm:<key>")，比率型（炼丹/修炼/渡劫/突破加成）由各系统查 power_of()；纯增益、挂机友好
- **突破率比例式**：`base × (1 + 0.15%×体质 + 0.10%×悟性 + 0.05%×气运) − 丹毒 + 丹药 + 神通`——旧加和式被 clamp 顶格吞掉三维差异，比例式让三维后期依然可解释
- **顿悟按每 4 时辰判定**：挂机 cultivate(24) 与手动 cultivate(4)×6 收益一致（原实现一次打坐一骰，挂机收益只有手动 1/6）
- **仙界产出**：修炼产出每境 ×3（凡界 ×1.6），顿悟比例 8%→12%（仙缘）

### 内核改动
- `config/realms.py`：仙界九境 + RealmDef.tribulation + POWERS/PowerDef/power_of + 仙界铭文/尊号
- `core/cultivation.py`：比例式成功率、仙界产出段、顿悟每 4 时判定 + inf 保护、飞升横幅、神通注入
- `systems/tribulation.py`：劫名数据驱动 + 劫体加成；`systems/alchemy.py`：丹心神通入算式；`core/game.py`：移除寿元死亡
- `ui/panel.py`：面板加神通行；`demo.py`：4.12 飞升演示段；`scripts/simulate_realms.py`：新建境界节奏模拟

### 验证
- **141 项测试全过**（原 126 + 仙界 9 + 公式 4 + 挂机 2）
- demo 4.12 飞升段：横幅/铭文/仙体神通/仙界面板全链路 ✓，存档一致性 ✓
- 冒烟：地仙→天仙真实链路「天衰之劫感应 73.1%」→ 渡劫 +5% → 突破独立判定（30.0% = 0.11×2.27+0.05 自洽）
- 长线模拟（seed=2026 纯挂机）：150 日飞升人仙 → 1474 日混元 → ~3000 日混元圆满；**仙界每境 130~190 日与凡界后期同构**

### 后续
第八批候选：**挂机与离线收益**（原计划，现在定位更清晰）→ **图鉴收集系统**（传记/见闻录/神通图鉴）。仙界专属内容（仙阶功法、仙界秘境、法则系统）是仙界数字爬坡之外的下一片空白。

## 第八轮：闭关挂机 + 离线收益

用户指令：「打坐可以自动打坐，就算挂机了也一直打坐就行了，收益就不要区分了。」——把打坐本身自动化，离线也持续打坐，收益与手动完全一致。

### 机制
- **闭关**：`idle [天数]` 自动推演「打坐→精力耗尽→休息→继续」，内部复用同一套 cultivate/rest——**收益不区分是硬约束**（无独立挂机公式，只有循环调度）；无参闭关至修为满自动停（突破留给玩家）；上限 30 日
- **CLI 空回车 = 自动打坐 4 时辰**：在线挂机连按回车即持续修炼
- **离线收益**：读档按真实流逝时间结算（1 现实小时 = 1 游戏日，`OFFLINE_DAYS_PER_HOUR` 可配，上限 24 时）；复用已有 `saved_at` 字段，老档零改动；结算后重写存档刷新时间戳，**反复读档不重复结算**
- 闭关/离线期间顿悟、功法熟练度照涨（同一订阅），不自动嗑药、不自动冲关

### 落地文件
- `core/cultivation.py`：`idle()` + `idle` 命令 + `IDLE_MAX_DAYS`
- `factory.py`：`settle_offline()` + `load_game` 接入 + `OFFLINE_DAYS_PER_HOUR/OFFLINE_MAX_HOURS`
- `ui/cli.py`：空回车打坐 + help 条目；`demo.py`：4.13 闭关段

### 验证
- **148 项测试全过**（+闭关 4 + 离线 3）；demo 4.13 + 一致性 ✓
- 冒烟：元婴 `idle 5` → 闭关 5 日 +2432 修为；存档拨回 2 小时读档 → 「离线归来 2 时辰（约 2 日）」+18698 修为 + 顿悟 2 次；二次读档零结算

### 后续
第九批候选：**图鉴收集系统**（传记/见闻录/神通图鉴）→ 仙界专属内容（仙阶功法、仙界秘境、法则系统）。

## 第八轮（二）：现实时钟——在线 1.5 倍 / 离线 1 倍

用户追加：「在线时间按 1.5 倍算，离线时间按 1 倍算……重点是全程要跟着现实时钟走。」

### 机制
- **时间模型升级**：手动操作命令推进；命令间隙 ≥1 分钟 → 现实时钟接管（`_settle_online`，在线倍率 1.5）；关游戏 → 读档离线结算（倍率 1.0）。三条路径复用同一套 idle→cultivate，收益天然一致
- `idle()` 支持小数天（float，按 `day×24+hour` 精确推进，打坐/休息时长按剩余目标截断）
- 常量：`ONLINE_DAYS_PER_HOUR=1.5`、`OFFLINE_DAYS_PER_HOUR=1.0`、`CLOCK_MAX_HOURS=24`（factory.py 顶部可调）
- 不足 1 游戏时辰不结算（操作间隙零打扰）

### 验证
- 153 项测试全过（+3 现实时钟 +2 闭关小数）；`settle_realtime(7200s,1.5)` 精确推进 72 时辰

### 后续
第九批候选：**图鉴收集系统** → 仙界专属内容（仙阶功法、仙界秘境、法则系统）。

## 第八轮（三）：后台周天——并行被动修炼

用户指令：「可以后台自动挂机吗？就是进行其他操作，不影响打坐。」

### 机制
- **被动修炼**：订阅全局 `hour_passed`，任何时间推进（战斗/秘境/赶路/炼丹/休息）自动涨修为 = 主动打坐 × 60%，不耗精力、不打断操作、静默无日志
- **主动打坐仍是高收益**：100% + 顿悟 + 熟练度；`_active_meditating` 标记防主动+被动 1.6 倍叠加
- `_hourly_gain(p, rng)` 公共产出函数：主动带随机、被动取期望

### 验证
- 157 项测试全过（+4 被动）；3 场战斗后台 +15~17/场、战斗零干扰；赶路 6 时 +39.4 vs 打坐 6 时 +60.6（60% 成立）

### 后续
第九批候选：**图鉴收集系统** → 仙界专属内容（仙阶功法、仙界秘境、法则系统）。

## 第九轮：数值防膨胀（大数格式化 + 平衡收口）

用户指令：「优化一下修为平衡，防止修为数值膨胀。」

### 结论先行
复测纯挂机（3000 日从炼气 → 凡界全程 + 仙界五境），仙界每境 ~190 日与既有基线一致——节奏未失控；顿悟按需求比例（8%/12%）无正反馈膨胀。**膨胀问题在显示层**（60,000,000,000 长串数字）。

### 落地
- `core/numfmt.py`：`fmt_num` 中文单位（万/亿/万亿/兆/京）
- 全显示层应用：panel（面板 8 处）、game.status_lines、cultivation 8 处日志、effects exp 日志、combat 13 处战斗日志
- 纯显示层改动，数值/判定/存档零影响

### 三层防膨胀机制
1. 显示：大数格式化
2. 数值：顿悟/秘境/失败惩罚全按需求比例（无正反馈）
3. 节奏：现实时钟 24 时上限 + 闭关 30 日上限 + 被动无独立公式

### 验证
- 161 项测试全过（+4）；渡劫后期面板「气血 8000万/修为 60亿」可读；demo 一致性 ✓

### 后续
第十批候选：**图鉴收集系统** → 仙界专属内容（仙阶功法、仙界秘境、法则系统）。

## 第十轮：Web 版（手机浏览器试玩）

用户问「做成安卓版本吗？如何测试？」——方案：Web + 手机浏览器（最优路径，APK 后置）。

### 落地
- `web_server.py`：纯标准库 HTTP 服务 + 深色修仙风页面（状态面板/日志流/输入框/30s 自动刷新）
- `CLI.run_line`：redirect_stdout 捕获输出，Web 与终端共用同一分发逻辑
- `./run.sh web` 一键启动；测试 161→164（TestWebEntry 3 项）

### 验证
- 三个 API 端点 HTTP 实测通过；页面即测试入口（status 也结算挂机收益）

### 如何测试（回答用户）
1. 自动化：`python -m unittest discover -s tests`（230 项）
2. CLI：`python main.py`
3. Web/手机：`./run.sh web` → 手机同 WiFi 浏览器开 http://<IP>:8000
4. 演示：`python demo.py`

### 后续
第十一批候选：**仙界专属内容**（仙阶功法/仙界秘境/法则）→ 图鉴收集。

## 第十一~十八轮（速记索引）

第十轮之后的八批落地，完整细节见 `docs/roadmap.md` 第十八节与 `.workbuddy/memory/` 工作日志：

| 轮次 | 主题 | 核心成果 |
|---|---|---|
| 十一 | 精力 = 每日行动预算 | 主动受限挂机不受限；打坐-休息自动衔接 |
| 十二 | 方案 A + 审计全量修复 | 产出统一按需求比例；战斗/打坐收敛 1.13 倍；3000 日可通关；十项 P0~P2 修复 |
| 十三 | 修为五途径 + 离线引擎 | 论道/历练/丹药/秘境/师门（每日限额）；UTC 待领池离线收益 |
| 十四 | 可扩展功法系统 | 声明式效果类型 + 品阶预算自检 + 叠加规则 + 封顶 |
| 十五 | 修仙五要（法财侣地师） | BonusAggregator 全局聚合；洞府/产业/道侣/师承 |
| 十六 | 门派深化 | 七大门派、门派功法、职位修炼场地、灵室静修 |
| 十七 | 天骄榜 + 心魔劫 | 排名竞技（俸禄/每日限战/NPC 成长）；心魔三选一题库 |
| 十八 | 时间预算闸门 | 游戏时间 ≤ 现实时间 × 10；advance_time 入口截断 |

测试数推进：164 → 185 → 195 → 203 → 214 → 223 → 230。

## 第十九轮：Web 页面重构（双栏 + 叙事文字流）

用户诉求：Web 页面重构——状态面板与修行历程分栏呈现，历程按发生顺序成为叙事流。

### 落地
- **双栏布局**（≥900px）：左栏角色卡片（境界/三槽进度条气血灵力精力/四维属性格/悟性根骨气运丹毒/神通功法装备标签）+ 行动按钮 + 指令输入；右栏「历程 · 文字流」固定高度滚动区
- **文字流**：每条修行记录带游戏时间标记（`POST /api/command` 返回 `time` 字段）与命令回显；按发生顺序逐条追加，淡入动画
- **分类着色**（`classify()` 纯前端关键词匹配）：过程叙述斜体灰、结果行（修为 +/突破/拾获/顿悟…）金色、失败行（失败/不足/溃败…）暗红、系统行灰色小字——与游戏逻辑零耦合
- 行动按钮状态自适应保留：机缘待决直出选项按钮、修为满变突破、精力不足变休息

### 验证
- 230 项测试全过（纯展示层重构，`GameSession`/`player_data` 契约不变，无新增测试）
- 页面解析核验：双栏布局 / 文字流分类 / 时间标记 / 命令响应时间全部就位

### 后续
第二十批候选：仙界专属内容（仙阶功法/仙界秘境/法则）→ 图鉴收集。

## 第二十轮：v2 挂机养老版（16 项变更全部落地）

用户给出《玩法设计文档 v2》：定位「挂机养老 · 不氪不肝 · 温和失败 · 法无止境」，并附 16 项与 v1 的变更清单。本批按文档逐项落地。

### 落地（对照 v2 变更清单，16/16）

- **收益时钟重构**：离线 0.12%/h + 神通加成只生效 50%；待领池上限=本层需求 100%，超出 30% 转灵石；互斥规则明确（离线独享/在线取高/主动替换被动）
- **精力放宽**：恢复 2/时（离线也恢复），消耗全面下调（打坐 1.5、猎妖 8、论道 5、探索 10、炼丹 6/炉、秘境 12/层）——精力只限主动操作频率，不限挂机收益
- **突破温和化**：失败只散当前层 20~40% + 轻伤（调息 4 时愈）+ 补偿 +2pp 至 +10pp，绝不掉境界；加成来源各封顶（丹药 15/功法 10/指点 5/合计 30pp）
- **软上限全覆盖**：修炼速率 +80% 超出算 0.3%、顿悟钳 15%、灵石 +300% 超出算 20%、突破率 +30pp 超出无效
- **防套利与防刷**：坊市每日买卖各 3 次 + 交易影响价格；凶险取消改探索疲惫（连续 3 次机缘减半，休息 4 时恢复）
- **新系统**：日常 7 项（全可跳过，3/5/7 档奖励）+ 一键托管 idle-auto；心魔加「随缘」选项；天骄 NPC 与玩家同步成长；门派贡献每日自动；产业/洞府自动维护；道侣边际递减 100/60/30/15%

### 验证
- **247 项测试全过**（+17：日常/托管/疲惫/软上限/温和失败/防套利/待领池/凶险取消）
- 纯挂机基线（seed=2026）：738 日飞升、1501 日混元圆满——精力放宽让纯挂机节奏比方案 A 快 2 倍
- 修复 `simulate_realms.py`：补时间闸门旁路（推演脚本必须 `clock.disabled=True`，记忆坑 #3）

### 后续
第二十一批候选：**数值节奏校准**（对照 v2 目标表调 `MEDITATE_EXP_RATIO`/离线速率/顿悟比例）→ 仙界专属内容。

## 第二十一轮：Web UI 组件化 + 响应式重构

用户要求：完善统一 Web 端 UI，响应式布局 + 组件化，抽象公共组件/样式变量/设计规范供移动端复用；动手前先说明改进点与方案。

### 诊断（5 个改进点）
样式零抽象（硬编码散落）/ 断点单一（仅 900px，<380px 溢出）/ 非组件化（大段拼接，无反馈层）/ 触控弱（无 44px 标准，hover 粘住）/ 难维护（1000+ 行内嵌服务端）。

### 落地
- **设计令牌** `xiuxian/ui/web/tokens.css`：颜色/间距(8pt)/字号/圆角/阴影/z-index 全 CSS 变量——移动端复用基础
- **组件样式** `app.css`：九组件（StatusCard/StatBar/StatGrid/TagList/ActionBar/CommandInput/FlowLine/Toast），零裸值
- **组件逻辑** `app.js`：`window.UI` 组件 API（renderStatus/renderActions/appendFlow/toast/classify/sendLine）
- **骨架独立** `page.html` + `web_server._load_page()` 启动内嵌，保持单文件零依赖
- **三档响应式**：≥1024 双栏 380px / 768-1023 双栏 320px / <768 单栏 + 底部固定行动条；按钮 ≥44px；hover 仅 hover 设备；错误走 Toast

### 验证
- 247 项测试全过；demo 一致
- playwright-core 5 视口断言：320/375 单栏+底部条+无溢出；768/1280/1600 双栏+底部条隐藏——全通过
- 交互链路：状态卡/点按钮/指令回显/Toast/文字流分类/30s 刷新——全通过
- 修复 1 个布局 bug：bottom-actions 默认 display:none，仅 <768px 显示

### 后续
第二十二批候选：**数值节奏校准**（对照 v2 目标表调 `MEDITATE_EXP_RATIO`/离线速率/顿悟比例）→ 仙界专属内容。

## 第二十二轮：Web 文字流式输出

用户要求：「文字采用流式输出」——叙事文字流改为打字机逐字呈现。

### 落地
- `ui/web/app.js`：`appendFlow` 改为队列驱动打字机（逐字渲染，默认 15ms/字，`window.UI.stream` 可调 enabled/charMs/fastThreshold）
- **防卡顿**：单批 >6 行或已有排队/打字中 → 直接全文渲染（闭关/战斗批量日志不卡）；超长行（>160 字）直接呈现
- **可打断**：点击日志区立即完成当前行；新命令自动排队衔接；滚动跟随保持底部
- 纯展示层增强，CLI/游戏逻辑零改动；测试 247 全过；playwright 验证打字机证据（120ms 时 4/14 字）/批量直渲/排队防卡/点击跳过 全通过

## 第二十三轮：离线速率校准

用户要求继续数值节奏校准。发现 v2 文档内部三处数值冲突：收益表 0.12%/时 vs 软上限表「离线=主动的 50%」vs 目标表 180-240 天飞升——0.12% 与后两者差 8 倍。

### 落地
- `OFFLINE_EXP_RATIO_PER_HOUR` 0.0012 → 0.0100（1%/现实时，对齐软上限表「离线=主动的 50%」）
- 测试断言更新（24h 离线 = 24% 需求）；README 三处同步（互斥规则表/结算公式/时间闸门/v2 基线，补矛盾说明）

### 量级核实
- 纯离线每层 ≈4.1 天，飞升 41 层 ≈170 天（落在 v2 目标 180-240 区间）
- 每日 1h 在线 ≈140 天（目标 90-120，需丹药/秘境/顿悟加成）
- v2 目标表「筑基 5-7 天」与「飞升 180-240 天」在比例体系下不可能同时满足，以飞升行为主锚

### 验证
- 247 项测试全过；真实存档离线链路冒烟通过（24h → 24 点 = 24% 需求）
- 算术一致性：纯离线飞升 ≈170 天，与目标表飞升区间吻合

### 后续
第二十四批候选：仙界专属内容（门派/秘境/炼丹扩展，仙劫链路完整性验证）。

## 第二十四轮：Web 操作机制优化（按钮优先）

用户要求：「完善操作机制，尽量避免通过输命令进行下一步。」

### 诊断
`GameSession.actions()` 返回静态按钮，未识别玩家卡住的场景。心魔劫 pending、秘境进行中、背包可用丹药、可进入秘境目录全没被覆盖，玩家需手敲 `demon 1`/`dungeon enter <id>`/`use <id>`。

### 落地
`actions()` 重写为上下文感知优先队列：心魔劫（突破后四选一）> 秘境进行中（深入/退出）> 待决机缘（`择·XX`/choose N）> 可突破（突破/休息/调息）> 背包可用丹药（「服 聚气丹」）> 可进入秘境（「秘境·落云秘境」，is_locked 动态过滤）> 休闲动作。输入框保留作兜底。

### 验证
- 251 项测试全过（+4 个 Web 动作测试）
- 跨境界 is_locked 过滤正确（炼气 only 落云 / 筑基 +万兽 / 金丹 +太虚洞天）
- playwright：移动端底部条 17 按钮横向滚动、点「闭关打坐」日志 1→10——通过
- 命令冒烟全正常

### 边界说明
二层「查看目录后再选」命令族（坊市/功法/洞府）保留查看型入口 + 输入框兜底；待决/卡住点已 100% 按钮化。

### 后续
第二十五批候选：仙界专属内容（门派/秘境/炼丹扩展，仙劫链路完整性验证）。

## 第二十五轮：仙界内容填坑（秘境/功法/地点/丹药/装备一次性全做）

### 背景
凡界 24 批打磨完后，仙界九境是「数值衔接但内容空白」的断层：境界/神通/铭文/劫名都有，唯独缺配套（秘境=0、仙阶功法=0、仙界地点=0、仙阶丹药=0）。正是第 24 轮「后续」挂着的候选。

### 需求确认
用户连续三次拍板：① 主攻方向四选全（数值/仙界/Web/文档）；② 基调「在线够用，数值只微调，重心仙界」；③ 仙界内容一次性全做。

### 落地（全部数据驱动，零核心数值改动）
| 模块 | 文件 | 内容 |
|---|---|---|
| 仙界秘境 | `config/dungeons.py` | 瑶池仙园（人仙,5层）/ 万劫云海（金仙,5层）；`_T_IMMORTAL`/`_T_MYSTIC` 宝箱池。守关+宝箱修为全 `exp_ratio`，灵石锚定入门境界一场战利量级 |
| 仙阶功法 | `config/arts.py` | 太虚剑典(攻伐112点)/周天妙法(修炼260点)/破虚诀(突破80点)/混元一气诀(全能296点,taiyi)。走已预留「仙 480」预算，`validate_balance` 全绿 |
| 仙界地点 | `config/realms.py` + `core/game.py` | 瑶池(4.5)/万劫云海(6.0) 带 `min_realm` 门槛；**修潜伏 bug**：`travel` 原用 `self.location_info()` 读当前所在地（错），改读目标地点 `LOCATIONS[location]` |
| 仙界丹药 | `config/items.py` + `recipes.py` | 蕴仙丹(修为8%需求)/破境仙丹(突破+15pp~6pp跨9境)；2 张仙阶丹方 + 仙材（仙兽内丹/仙草） |
| 仙阶装备 | `config/items.py` | 六部位，`min_realm="human_immortal"`，掉落池按境界自动筛 |
| 面板修复 | `ui/panel.py` | `locations_panel` 原 `info['danger']` KeyError（现有 LOCATIONS 无 danger 键），改 `.get()` 兜底 + 显示仙界门槛 |

### 验证
- **262 项测试全过**（原 251 + 11 项 `TestImmortalContent`）
- 品阶预算自检全绿；仙界秘境进/抽签/推进/通关链路跑通；存档读档兼容（`load_state` 按 DUNGEONS 白名单识别新 id）
- 仙界节奏模拟（人仙起步纯挂机）：跨大境界约 25~46 日，无 inf/死循环

### 遗留
- `simulate_realms.py` 既有缺陷（概率按最终状态快照 + 层内重复记录），本次未动
- 凡界事件 `events.json` 仍用绝对 `exp`（260/700），仙界入口已全 `exp_ratio`；凡界事件不在本次范围
- 仙界法则系统/仙阶门派/仙界专属事件是下一片空白

## 第二十六轮：Web/移动端打磨（仙界内容前端落呈现 + 移动端收尾）

### 背景
承接第二十五批仙界内容填坑——内容（秘境/功法/地点/丹药）已落地，但前端只靠行动按钮自动过滤（人仙自现「秘境·瑶池仙园」），缺「仙界身份感」与结构化浏览入口。用户在主攻方向选了「Web/移动端打磨」，本轮确认「两者都做」。

### 需求确认
用户选「两者都做（推荐）」：仙界内容前端落呈现（主体）+ 移动端交互打磨（顺带收尾）。

### 落地
| 层 | 文件 | 内容 |
|---|---|---|
| 服务端 | `web_server.py` | `player_data()` 增 `realm_is_immortal`/`realm_family`（仙界身份）+ `arts` 数组（装备功法带 rank）；新增 `catalog_data()` → `/api/catalog`（秘境目录结构化 JSON，`state` 复用服务端 `is_locked()` 单一事实源）；`actions()`「秘境」升级「秘境目录」带 `open_catalog` 标志 |
| 前端 | `ui/web/app.js` | 状态卡 hero 加「仙」金徽章(`realm-badge immortal`)/「凡」灰徽章；功法标签读 `d.arts`，仙阶金色高亮+品阶标注；新增 `openCatalog()` 弹窗（卡片式五座秘境，仙阶金边+「仙」徽章，可入项 `tap` 点直接 `dungeon enter`，锁定项显红色门槛原因）；`window.UI` 导出 `openCatalog` |
| 前端样式 | `ui/web/app.css` | `.realm-badge`（仙金/凡灰）+ `.catalog-*`（目录卡片/状态/徽章/提示）+ 移动端适配 |
| 移动端 | `ui/web/app.css` 响应式区 | 行动条 `.btn` 加 `white-space:nowrap` 防溢出；`.btn.catalog` 置顶；弹窗 `.modal{max-height:92vh}` + `.modal-body` 独立滚动 |

### 本批修的 bug（重要，入经验手册 #33）
- `openCatalog` 里 `const cls` 后 `cls += ...` 给常量赋值 → `TypeError: Assignment to constant variable`，被函数内 `catch` 吞掉只 toast「加载失败」（fetch/json/map 都好的，纯 JS 语法级错误）。
- 排查手法：`agent-browser eval` 分步复刻（fetch→json→map）定位到具体语句，而非看 toast 文案猜。

### 验证（agent-browser 端到端）
- **262 项测试全过**（展示层改动，无后端回归）；`/api/status` 新档 family=凡界含 `open_catalog`；`/api/catalog` 5 座秘境正确
- 桌面截图：凡界「凡」徽章 + 秘境目录弹窗（可入绿/锁定红/仙阶「仙」金徽章）
- 前端单测（eval 模拟仙界玩家数据）：`realm_is_immortal=true` → 仙徽章 + 仙阶功法金色标签正确（`功法·周天妙法（仙阶）`）
- 端到端交互：弹窗点落云秘境 → `dungeon enter` → 行动区变「深入 · 落云秘境 1/3」
- 移动端（iPhone 14 设备模拟）：单栏堆叠 + 底部行动条 + 弹窗 92vh 全屏适配，无横向溢出

### 后续
- WebView 打包评估（把当前 Web 版封装成移动 App 壳）——README/overview 均标注为「安卓版最优形态」候选
- 仙界第二片空白：法则系统 / 仙阶门派 / 仙界专属事件

---

## 第二十七轮：仙界深度重构（法则 · 仙门 · 仙界事件）

用户选「三块全做」+「深度重构仙界成长曲线」，并锁定三项数值目标：仙界约 1170 天 / 法则分维度 + 预算限制 / 软门槛不禁突破。

### 先诊断，后动手

不做需求就直接写码，先跑模拟取实测数据：

- 凡界九境 **669 天（74 天/境）**，仙界九境 **592 天（74 天/境）** —— **完全同速**，飞升毫无体感差异
- 根因是数学层面的：产出挂「本层需求比例」⇒ 每层耗时 = 需求 ÷(需求×0.002×密度) ≈ **常数**，仙界密度↑（4.5~6.0）被 `exp_growth`↑（2.2~3.0）恰好抵消
- 结论：**硬拉 `exp_growth` 无用**，必须加第二条成长轴

### 方案：双轴成长

境界轴（打坐→修为→加区）+ 法则轴（悟道→感悟→**乘区**）。法则走乘区是刻意的：走加区会被境界指数成长稀释，玩家感受不到差异。仙界突破改双条件（修为 AND 法则门槛），仙界打坐 13~15 天即圆满，剩余时间全在悟道——「仙途漫漫」由玩法产生，不是数值硬拉。

### 交付

| 模块 | 内容 |
|---|---|
| 法则系统 | 八条 × 五阶，`wudao` / `laws` / `law focus` 命令，面板可查阶数与门槛进度 |
| 突破软门槛 | 未达标 −30pp（终局 −35pp），压到 5% 下限但**不禁**，可硬冲 |
| 仙阶门派 | 5 座仙门，主修法则悟道 +50% / 兼修 +20%；仙职五阶；飞升后凡界宗门「仙凡两隔」但 buff 保留 |
| 仙界事件 | 12 个（事件总数 15 → 27），仙界触发占比实测 94% |
| 法则顿悟 | 因果法则的第二处消费点，静悟时概率触发 |

### 过程中发现并修复的三个空洞

1. **混元境只停留 8 天** —— `should_keep_wudao()` 在「无下一境」时 early return，末境完全不悟道。补终局门槛（34 阶）后 **8 天 → 135 天**。
2. **因果法则两阶触顶** —— 它给 `insight_rate`，而打坐顿悟硬钳 15%、基础 5%+气运 0.8%，点两阶就满，后三阶白点。新开「法则顿悟」作为第二处消费点。
3. **事件感悟必然崩盘** —— 初版按「下一阶需求百分比」给，后期 6000×12% = 720 = **15 天产出**，一次探索顶半个月。改挂**产出速率**（`insight_hours`）+ 每日硬闸，总预算压到基准的 3.25%，写成测试守住。

### 校准踩的坑（值得记住）

调 `WUDAO_BASE` 时发现 1.75→1.82 实测反而从 1212 天变成 1025 天（方向反常）。查下来是**单种子模拟噪声 σ≈145 天，已盖过参数差异**——等于在噪声上过拟合。改成 **8 种子取均值**才校准成功。

| 阶段 | 仙界总时长 | 偏差 |
|---|---|---|
| 初版 | 1132 日 | −3.3% |
| + 法则顿悟 | 1043 日 | −10.9% |
| 反解 1.75 | 1212 日 | +3.6%（但大罗 235 日超标） |
| **1.82 + 终局门槛** | **1204 日** | **+2.9%**（σ=143，落在噪声内，停止精调） |

### 结果

- **305 项测试全过**（原 262 + 新增 43），零回归
- 凡界 68.6 天/境 → 仙界 **104.8 天/境**（比值 **1.53x**，重构前均为 74）
- 仙门悟道加速实测 1.51（设计值 1.50）
- 存档**向前兼容**：剥离新字段的老档可读可玩，零法则进度直接上手

### 后续

- 仙界师承（仙门师长指点）尚未做，现仙门只有悟道加速与乘区
- 仙界专属秘境仅 2 处，可再扩
- 法则第五阶（主宰）为长线白金追求，暂无专属内容
